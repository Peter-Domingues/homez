import PropertyInfoComponent from "@/components/propertyInfo";

export const metadata = {
  title: "Property | Anastasia Benedeti",
};

const PropertyInfoPage = ({ params }) => {
  return <PropertyInfoComponent />;
};

export default PropertyInfoPage;
